# Also search these file types
--type-add=ruby=.builder,.feature,.gemspec,Gemfile
--type-add=css=.scss
--type-add=html=.ejs

# Ignore the tmp directory
--ignore-dir=tmp

# Set paging with color
--pager=less -FRX
