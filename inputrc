# ~/.inputrc is read by readline, which is used by bash and other many programs

# Ignore case when <tab> completing
set completion-ignore-case on

set completion-prefix-display-length 2

# Immediately add a trailing slash when autocompleting symlinks to directories
set mark-symlinked-directories on

# Vim keybindings in programs that use readline
set keymap vi
set editing-mode vi

# Map vim-style CTRL-k and CTRL-j to move up and down history
# https://www.gnu.org/savannah-checkouts/gnu/bash/manual/bash.html#Commands-For-History
"\C-k": previous-history
"\C-j": next-history
