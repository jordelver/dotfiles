# Ruby/Rails
*.gemspec diff=ruby
*.ru diff=ruby
*.rb diff=ruby
*.rake diff=ruby
Gemfile diff=ruby
Rakefile diff=ruby
*_spec.rb diff=spec
Gemfile.lock merge=binary

# Elixir
*.ex diff=elixir
*.exs diff=elixir
mix.lock merge=binary

# JavaScript
package-lock.json merge=binary
yarn.lock merge=binary
